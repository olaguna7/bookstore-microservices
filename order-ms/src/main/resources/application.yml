server:
  port: 8083
spring:
  datasource:
    url: jdbc:h2:mem:orders_db
    driver-class-name: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true